# keyword-updater
This program simply iterates through all .xlsx files in a given directory and updates all instances of a keyword with an associated definition (given a dictionary file of associations).

## Development
